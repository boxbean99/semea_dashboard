# SE&MEA Dashboard — 세션 Handover
**작성일**: 2026-04-02  
**다음 세션 시작 위치**: Phase 3 (Project Tracker) — 또는 To-Do 후속 수정

---

## 프로젝트 기본 정보

| 항목 | 내용 |
|------|------|
| GitHub | https://github.com/boxbean99/semea_dashboard |
| 로컬 경로 | `C:\Users\User\.claude\semea_dashboard_repo\` |
| 배포 | Vercel (미연결) |
| Supabase | https://supabase.com/dashboard/project/mlxnwcwgvlhjmrcavaok |

---

## 기술 스택

| 항목 | 버전/선택 |
|------|-----------|
| 프레임워크 | Next.js 16.2.2 (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS v4 (CSS-first, `@theme` 블록) + 인라인 스타일 |
| 폰트 | Space Grotesk (next/font/google) |
| 데이터 패칭 | @tanstack/react-query v5 |
| DB | @supabase/supabase-js v2 (클라이언트 설정 완료, 테이블 미생성) |
| 차트 | recharts v3 |
| 아이콘 | lucide-react v1.7 |

---

## 완료된 작업

### Phase 0 — 인프라 ✅
- Next.js 프로젝트 초기화 + 의존성 설치
- Space 테마 디자인 시스템 (`globals.css` — `@theme` 블록)
- 3탭 네비게이션 (`/dashboard`, `/todo`, `/tracker`)
- QueryProvider, Supabase 클라이언트
- Mock 데이터 구조, 타입 정의, 도시/국가/팀원 config

### Phase 1 — Dashboard ✅
- **Overview**: 남부유럽팀 누적 KPI 8종 + Sparkline + 지역분할 테이블
- **City Detail**: 국가 pill 탭 → 도시 underline 탭 → KPI 8종
- **DetailTabs**: 카테고리별(바차트+테이블) / 상품별(검색+정렬+페이지네이션) / 유입채널(placeholder)
- **KPI 카드**: 월간 MoM / 주간 WoW / 일간 DoD + 전주 동요일 2단 비교
- **버그 수정**: Seeded RNG(`makeRng(seed)`)로 hydration 오류 해결

### Dashboard UI 수정 ✅ (이번 세션)
- 헤더 2줄 구조: 1행(남부유럽팀 + 새로고침) / 2행(탭)
- `전체/Europe/ME·Africa` region 탭 삭제
- Overview·CityDetail + 월간·주간·일간 한 줄 배치
- KPI 그리드 `repeat(4, 1fr)` 고정 4×2
- max-width 1200px 고정 컨테이너
- 폰트 1단계 전체 업사이즈 (body 14→15px 등)
- muted 텍스트 색 명도 개선 (`#4a5a7a` → `#7a8faa`)
- 조회 시점 날짜 텍스트 색 개선

### Phase 2 — To-Do List ✅ (이번 세션)
- **팀원 6명**: 도영·다현·도연·수연·New!·CLAUDE
- **PixelAvatar**: 12×16 픽셀 그리드 SVG — 캐릭터별 개성 있는 도트 상반신
- **MemberCard**: 아바타 + 이번 주 달성률 진행 바(레벨업 스타일) + 마감 임박 Top3 to-do
- **3×2 고정 그리드** 멤버 카드
- **팀 전체 달성률 진행 바**: 완료(초록)+진행 중(노랑) 2단 fill + 세그먼트 구분선
- **AllTodoList**: 정렬(우선순위>마감일>가나다) + 15개씩 더보기
- Mock 26개 todo (이번 주 2026-03-30~04-05 기준)

---

## 파일 구조

```
semea_dashboard_repo/
├── app/
│   ├── globals.css              ← Space 테마, body 15px
│   ├── layout.tsx               ← max-width 1200px 컨테이너
│   ├── page.tsx                 ← /dashboard 리다이렉트
│   ├── dashboard/page.tsx
│   ├── todo/page.tsx            ← Phase 2 구현 완료
│   └── tracker/page.tsx         ← Phase 3 stub
├── components/
│   ├── layout/Navigation.tsx    ← 2줄 헤더 (팀명+새로고침 / 탭)
│   ├── providers/QueryProvider.tsx
│   ├── ui/
│   │   ├── KpiCard.tsx          ← KPI 카드 (scope별 delta, 폰트 24px)
│   │   └── Sparkline.tsx        ← SVG 스파크라인
│   ├── dashboard/
│   │   ├── DashboardClient.tsx  ← view/scope 상태 (region 제거됨)
│   │   ├── DashboardControls.tsx← Overview/City + 월간/주간/일간
│   │   ├── TeamOverview.tsx     ← 남부유럽팀 누적 + 4×2 KPI 그리드
│   │   ├── RegionSplitTable.tsx
│   │   ├── CityDashboard.tsx    ← COUNTRIES 전체 사용 (region prop 제거)
│   │   ├── CategoryTab.tsx
│   │   ├── ProductTab.tsx
│   │   └── ChannelTab.tsx       ← placeholder
│   └── todo/
│       ├── PixelAvatar.tsx      ← 6종 12×16 도트 캐릭터 SVG
│       ├── MemberCard.tsx       ← 팀원 카드 (아바타+진행바+Top3)
│       └── AllTodoList.tsx      ← 전체 TDL 리스트 + 더보기
├── lib/
│   ├── mock/
│   │   ├── metrics.ts           ← Seeded RNG 기반 도시별 KPI
│   │   └── todos.ts             ← 26개 todo mock (6명)
│   ├── utils/
│   │   ├── format.ts
│   │   └── kpi.ts
│   └── supabase/client.ts
├── config/
│   ├── cities.ts                ← 14개 도시
│   ├── countries.ts             ← 6개 국가
│   └── members.ts               ← 6명 (도영·다현·도연·수연·New!·CLAUDE)
├── types/index.ts
└── .env.local                   ← NEXT_PUBLIC_SUPABASE_ANON_KEY 미입력
```

---

## 다음 세션: Phase 3 — Project Tracker (`/tracker`)

### 구현할 내용
- 프로젝트 카드 목록 (stage별 칸반 or 테이블)
- Stage: `planning / discussion / development / closing`
- 우선순위, 담당자, 기간(start~end), 설명
- 데이터: `lib/mock/todos.ts`의 `MOCK_PROJECTS` 사용 → 추후 Supabase 이관

### To-Do 후속 개선 (우선순위 낮음, 요청 시 처리)
- 팀원 카드 클릭 시 상세 모달 또는 확장 뷰
- Supabase `todos` 테이블 이관 (ANON_KEY 입력 후)
- todo 항목 인라인 편집/추가/삭제

---

## 알려진 이슈 / 주의사항

| 항목 | 내용 |
|------|------|
| `.env.local` | `NEXT_PUBLIC_SUPABASE_ANON_KEY` 미입력 — Supabase 연동 불가 |
| 이번 주 기준 날짜 | MemberCard·TodoPage에 `WEEK_START="2026-03-30"` 하드코딩 → 추후 동적 처리 |
| ChannelTab | BigQuery UTM 데이터 연동 전까지 placeholder |
| 기간 라벨 | DashboardControls의 날짜 하드코딩 → 추후 동적 날짜로 교체 |
| git 미커밋 | Phase 1 수정 + Phase 2 전체가 미커밋 상태 — 사용자가 직접 커밋/푸시 |
| Tailwind v4 | `tailwind.config.js` 없음, `globals.css`의 `@theme` 블록으로 관리 |

---

## 헤르미온느 운영 규칙

- 작업 경로: `C:\Users\User\.claude\semea_dashboard_repo\`
- `git push`는 항상 사용자가 직접 실행
- BigQuery 쿼리 호출 전 사전 공지 필수 (Portugal: Lisbon, Porto만 허용)
- 빌드 실패 시 커밋 금지
- Mock 데이터에 `Math.random()` 사용 금지 → Seeded RNG(`makeRng(seed)`) 사용
