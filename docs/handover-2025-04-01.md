# SE&MEA Dashboard — 세션 Handover
**작성일**: 2025-04-01  
**다음 세션 시작 위치**: Phase 2 (To-Do List)

---

## 프로젝트 기본 정보

| 항목 | 내용 |
|------|------|
| GitHub | https://github.com/boxbean99/semea_dashboard |
| 로컬 경로 | `C:\Users\User\.claude\semea_dashboard_repo\` |
| 배포 | Vercel (미설정, 추후 연결) |
| Supabase | https://supabase.com/dashboard/project/mlxnwcwgvlhjmrcavaok |

---

## 기술 스택

| 항목 | 버전/선택 |
|------|-----------|
| 프레임워크 | Next.js 16.2.2 (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS v4 (CSS-first, config 파일 없음) + 인라인 스타일 |
| 폰트 | Space Grotesk (next/font/google, 상업적 이용 가능) |
| 데이터 패칭 | @tanstack/react-query v5 |
| DB | @supabase/supabase-js v2 (클라이언트만 설정, 테이블 미생성) |
| 차트 | recharts v3 |
| 아이콘 | lucide-react v1.7 |

---

## 완료된 작업

### Phase 0 — 인프라 ✅
- Next.js 프로젝트 초기화 + 의존성 설치
- Space 테마 디자인 시스템 (globals.css — `@theme` 블록으로 컬러/폰트 변수 정의)
- 3탭 네비게이션 (`/dashboard`, `/todo`, `/tracker`)
- QueryProvider (React Query)
- Supabase 클라이언트 (`lib/supabase/client.ts`)
- Mock 데이터 구조 (`lib/mock/metrics.ts`, `lib/mock/todos.ts`)
- 타입 정의 (`types/index.ts`)
- 도시/국가/팀원 config (`config/cities.ts`, `config/countries.ts`, `config/members.ts`)

### Phase 1 — Dashboard ✅
- **Overview**: SE&MEA 팀 누적 KPI 8종 + Sparkline + 지역분할 테이블
- **City Detail**: 국가 pill 탭(알파벳순) → 도시 underline 탭 → KPI 8종
- **DetailTabs**: 카테고리별(바차트+테이블) / 상품별(검색+정렬+페이지네이션) / 유입채널(placeholder)
- **KPI 카드**: 월간 MoM / 주간 WoW / 일간 DoD + 전주 동요일 2단 비교
- **버그 수정**: `Math.random()` → Seeded RNG (`makeRng(seed)`)로 hydration 오류 해결

---

## 파일 구조

```
semea_dashboard_repo/
├── app/
│   ├── globals.css              ← Space 테마 컬러 변수
│   ├── layout.tsx               ← Space Grotesk + QueryProvider + Navigation
│   ├── page.tsx                 ← /dashboard 리다이렉트
│   ├── dashboard/page.tsx
│   ├── todo/page.tsx            ← Phase 2 stub
│   └── tracker/page.tsx         ← Phase 3 stub
├── components/
│   ├── layout/Navigation.tsx    ← sticky 헤더, 3탭
│   ├── providers/QueryProvider.tsx
│   ├── ui/
│   │   ├── KpiCard.tsx          ← 재사용 KPI 카드 (scope별 delta 표시)
│   │   └── Sparkline.tsx        ← SVG 스파크라인
│   └── dashboard/
│       ├── DashboardClient.tsx  ← 상태 관리 루트 (view/scope/region)
│       ├── DashboardControls.tsx
│       ├── TeamOverview.tsx
│       ├── RegionSplitTable.tsx
│       ├── CityDashboard.tsx    ← CountryTabs + CityTabs + KpiGrid + DetailTabs
│       ├── CategoryTab.tsx
│       ├── ProductTab.tsx
│       └── ChannelTab.tsx       ← placeholder
├── lib/
│   ├── mock/
│   │   ├── metrics.ts           ← Seeded RNG 기반 도시별 KPI
│   │   └── todos.ts             ← TDL/PJT mock 데이터
│   ├── utils/
│   │   ├── format.ts            ← formatKRW, calcDelta, comparisonLabel
│   │   └── kpi.ts               ← aggregateMetrics, KPI_DEFS
│   └── supabase/client.ts
├── config/
│   ├── cities.ts                ← 14개 도시 (region별 분류)
│   ├── countries.ts             ← 6개 국가 (알파벳순, 추후 정렬 변경 가능)
│   └── members.ts               ← 팀원 4명
├── types/index.ts
└── .env.local                   ← Supabase URL 있음, ANON_KEY 입력 필요
```

---

## 다음 세션: Phase 2 — To-Do List

### 구현할 내용
- `/todo` 페이지 구현
- 팀원 카드 4개 (상단 그리드)
- 각 팀원 클릭 → 개인 TDL 공간 (편집 가능)
- 상태: `할 일 / 진행 중 / 완료`
- 우선순위: `HIGH / MID / LOW / 상시`
- 관련 도시 태깅
- 데이터: `lib/mock/todos.ts` → Supabase `todos` 테이블 이관 예정

### Supabase 테이블 설계 (Phase 2 시작 전 필요)
```sql
-- todos
create table todos (
  id uuid primary key default gen_random_uuid(),
  member_id text not null,
  title text not null,
  status text check (status in ('todo','in_progress','done')),
  priority text check (priority in ('HIGH','MID','LOW','ALWAYS')),
  related_city text,
  due_date date,
  created_at timestamptz default now()
);
```

---

## 알려진 이슈 / 주의사항

| 항목 | 내용 |
|------|------|
| `.env.local` | `NEXT_PUBLIC_SUPABASE_ANON_KEY` 미입력 상태 |
| ChannelTab | BigQuery UTM 데이터 연동 전까지 placeholder 유지 |
| 기간 라벨 | DashboardControls의 "2025년 3월" 등 하드코딩 → 추후 동적 날짜로 교체 |
| BigQuery | Phase 1은 전체 Mock 데이터. 실제 연동 시 사전 공지 필요 |
| Tailwind v4 | `tailwind.config.js` 없음, `globals.css`의 `@theme` 블록으로 컬러 변수 관리 |

---

## 헤르미온느 운영 규칙

- 작업 경로: `C:\Users\User\.claude\semea_dashboard_repo\`
- `git push`는 항상 사용자가 직접 실행
- BigQuery 쿼리 호출 전 사전 공지 필수 (Portugal: Lisbon, Porto만 허용)
- 빌드 실패 시 커밋 금지
